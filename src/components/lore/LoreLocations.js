import React from 'react';

const LoreLocations = props => {
    return (
        <div className="Lore-Locations">
            LoreLocations
        </div>
    )
}

export default LoreLocations;